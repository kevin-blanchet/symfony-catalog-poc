{% extends 'base.html.twig' %}

{% block title %}Product{% endblock %}

{% block body %}
    {{ form_start(searchForm) }}
        <div class="d-flex">
            {{ form_widget(searchForm.search_term, {
                attr: {class: 'form-control form-control-lg'}
            }) }}

            <button class="btn btn-dark" type="submit">Search</button>
        </div>
    {{ form_end(searchForm) }}
{% endblock %}
